class CountryDetails {
  String lastDate;
  String confirmed;
  String deaths;
  String recovered;
  String active;

  CountryDetails(
      this.lastDate, this.confirmed, this.deaths, this.recovered, this.active);
}
